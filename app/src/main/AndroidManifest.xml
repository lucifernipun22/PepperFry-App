<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example">

    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PepperFry">
         <activity android:name=".SofasActivity"></activity>
        <activity android:name=".FindStudioActivity" />
        <activity android:name=".StartShoppingActivity" />
        <activity android:name=".MobileNumberVerificationActivity" />
        <activity android:name=".KidsRoom" />
        <activity android:name=".Bedroom" />
        <activity android:name=".Living" />
        <activity android:name=".Furniture" />
        <activity android:name=".HomeScreen" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".AlertsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>